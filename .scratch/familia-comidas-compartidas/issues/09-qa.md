# 09 — QA: demo en navegador + simulador + /senda-review

Status: claimed
Blocked by: 02, 03, 04, 05, 06, 07, 08

## Objetivo

Cerrar la feature con una pasada completa de extremo a extremo en las dos apps.

## Escenarios (todos con perfiles demo, nunca la cuenta real — memoria `verify-with-demo-profile`)

1. **Alta de familia.** Perfil A crea familia, se declara planificador, añade: perfil B (con
   app), "Abuela" (sin app), niño "Leo" (5 años, alergia al huevo). Perfil B mete el código,
   elige "soy B", queda dentro.
2. **Comidas compartidas.** A marca comida y cena compartidas todos los días, desayuno solo
   L–V. Genera el plan. B ve el mismo menú compartido en Hoy y en Plan sin generar nada.
3. **Compra por comensales.** La lista de A cubre raciones para A + B + Abuela + Leo en los
   slots compartidos; para el desayuno del finde (no compartido) solo la ración de A. Total
   dentro del presupuesto de la casa.
4. **Plato de niño.** Algún día con un plato compartido que llevaría huevo → Leo tiene plato
   aparte; sus ingredientes están en Ingredientes. A cambia el plato de Leo de mañana desde
   Hoy → literal, hoy intacto.
5. **Estado de compra compartido.** B marca ingredientes "en casa" y sube un tiquet → A ve
   las marcas y el gasto. B no puede regenerar ni cambiar cadencia ni editar `shared_slots`.
6. **Comidas en solitario de B.** B planifica sus desayunos (no compartidos) → aparecen solo
   en su Hoy/Plan y en su lista "Tu compra" a 1 ración; A no los ve.
7. **Cambio de plato compartido (D2).** B pide al coach "cámbiame la cena del jueves" →
   recibe "eso lo lleva A". A la cambia → cambia para A y B, hoy intacto. A hace
   `adjustMonthlyPlan` ("me pasé con la comida") → recoloca los futuros compartidos de los dos.
8. **Traspaso de planificador (D3).** A (planificador) sale del hogar → `is_planner` salta al
   miembro con cuenta de más edad (entre B y quien sea); ese pasa a ver la lista de la casa
   como suya y el espejo sigue.
9. **Privacidad.** El estado de comida de B ("comí distinto") y su peso no son visibles para
   A en ningún sitio.
10. **Datos antiguos.** Un hogar creado con el modelo viejo (antes de la migración 1) sigue
    funcionando: creador = planificador, miembro previo = hueco reclamado, `shared_slots`
    migrados.

## Puertas

- `bun run lint`, `bun run typecheck`, `bun run test` verdes.
- `/senda-review` sobre el diff completo (idioma, frontera cliente/servidor, espejo
  `/api/v1`, invariantes de plan/compra, paridad web/móvil, RLS).
- Capturas del simulador de: alta de familia, Hoy de un no planificador, Ingredientes con
  raciones de hogar, plato de niño.
- Actualizar `CLAUDE.md` y `AGENTS.md` (sección Familia/hogar) y `docs/agents/code-review.md`
  con las invariantes nuevas (planificador único, estado de compra compartido, `PlanDay.kids`,
  `shared_slots` a nivel de hogar).

## Hecho cuando

Los 8 escenarios pasan en web y móvil; puertas estáticas y `/senda-review` limpios; docs
actualizadas.

## Answer (parcial — 2026-09-01)

Sesión de QA sobre los issues 06+07+08 (sin commitear, en la rama
`familia-comidas-compartidas`).

### Hecho y verde

- **Puertas estáticas**: web `lint` 0 errores / `typecheck` limpio / **122 tests**;
  móvil `tsc --noEmit` limpio. `bun run format` pasado.
- **`/senda-review` del diff completo**: limpio en las reglas duras (idioma, frontera
  cliente/servidor, espejo `/api/v1`, invariantes de plan/compra, paridad web/móvil, RLS).
  3 hallazgos menores, ninguno bloqueante:
  1. `resolveShoppingRow` corría `householdContext` (3 consultas) en cada escritura de
     estado de compra solo para sacar `plannerId`. **Corregido**: helper nuevo
     `householdPlannerId` (una consulta) en `household.server.ts`.
  2. Rama de filtrado de `kids` en `blankSharedSlots` sin test (helper privado). Anotado,
     no corregido.
  3. `setChildMeal` / `cleanKids` aceptaban `slot: "snack"` pese a D5. **Corregido**: los
     dos acotados a `MEAL_KEYS` (desayuno/comida/cena); caso de test añadido a
     `plan-shared.test.ts`.
- **Docs**: sección "Familia — hogar compartido" nueva en `AGENTS.md` y en `CLAUDE.md`
  (resumen), y en `docs/agents/code-review.md` (checklist). Cubren: planificador único +
  traspaso D3, `shared_slots` a nivel de hogar, fila propia + espejo de lectura D1, estado
  de compra del hogar, `PlanDay.kids`, coach con contexto de hogar, y la trampa de RLS de
  `monthly_plans`.
- **Escenario 5 (estado de compra compartido) — verificado extremo a extremo** vía API +
  sonda de BD: como Marta (no planificadora, hogar `B27FAY3V`, planificador Alex),
  `POST /api/v1/plan/shopping-owned` y `/plan/pantry-extra` → las marcas caen en la fila
  `monthly_plans` de **Alex** (`pantry_extras`, `ownedTrips`), la fila de Marta queda
  intacta (`pantry_extras: null`), y `weekQty` / `plan` de Alex sin tocar. Datos de prueba
  revertidos después.
- **Issue 07 — pila de guardas de `setChildMeal`** verificada por el camino real: ruta
  espejo + auth + validador + resolución de niño por nombre + (guarda D2). El log del
  servidor confirma `apiPost Error: Ese niño no está en tu casa` para un no planificador
  cuyo hogar no tiene ese niño (la guarda D2 va justo después, mismo patrón que
  `guardSharedSlotWrite`, ya verificada en 05/08).
- **Escenarios 1/2/3/6/7/9/10** — cubiertos por la verificación por partes de sesiones
  anteriores (ver `familia-comidas-compartidas-progress.md`) + revisión de código de esta
  sesión: alta de familia + roster + join en dos pasos, rejilla de comidas compartidas
  planificador-editable / no-planificador-lectura (confirmado también en el simulador esta
  sesión: Copiloto ve la rejilla en lectura, "Lo decide Nacho"), plan en solitario del no
  planificador, rechazo del coach sobre una comida compartida, privacidad del estado de
  comida y el peso, backfill del modelo viejo.

### Pendiente (bloqueado por el entorno de esta sesión)

El panel del navegador **no aceptaba clics ni escritura** (todas las llamadas a `computer`
expiraban con "Browser pane is currently hidden"); lecturas y JS sí funcionaban. El
simulador de iOS aceptaba taps de forma intermitente y no se llegaba a la barra de
navegación desde la pantalla Familia. Con eso:

- **Escenario 4 — camino feliz del plato de niño**: la IA emitiendo `days[].kids` al
  generar, el planificador poniendo el plato de un niño desde Hoy/coach, y el espejo a los
  demás miembros. NO verificado en vivo (requiere estar logueado como planificador; los
  planificadores de prueba son perfiles demo no reautenticables). Cubierto solo por los
  tests unitarios (`cleanKids`, `childMealsForDate`, `composeDayForUser`+kids,
  `mergeFuturePlan`+kids) y la revisión de código.
- **Escenario 8 — traspaso de planificador (D3)**: es un trigger `AFTER DELETE` de la
  migración del issue 01 (aplicada por el usuario). NO probado en vivo esta sesión.
- **Capturas del simulador**: solo se tienen las de Familia de un no planificador (web +
  iOS) y Plan de un no planificador (iOS). Faltan: plato de niño, Ingredientes con
  raciones de hogar, alta de familia.
- **iOS**: pasada del simulador para 07 y 08 (ya pendiente de sesiones anteriores).

### Recomendación

Cerrar 04/08 y las capturas en una sesión con el panel del navegador operativo, o
directamente por el usuario. El resto de la feature (código + invariantes de backend +
docs) queda verificado.
