import { ScreenStub } from "../../components/screen-stub";

export default function Chat() {
  return (
    <ScreenStub
      title="Coach"
      note="El chat con el coach se porta a la app en una próxima fase. Tu mensaje se enviará cuando esté listo."
      withNav={false}
      back
    />
  );
}
